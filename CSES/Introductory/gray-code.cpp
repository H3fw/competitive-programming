#include <bits/stdc++.h>

using namespace std;

#define arr std::array
#define ll long long

void solve() {
  int n;
  cin >> n;

  vector<string> greycode = {"0", "1"};
  for (int i = 0; i < pow(2, n); i++) {
    continue;
  }

  int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    solve();
    return 0;
  }
