#include <bits/stdc++.h>

using namespace std;

#define arr std::array
#define ll long long
#define int ll

vector<vector<int>> lcs;

void solve() {
  int s, t;
  cin >> s >> t;

  lcs.resize(s, vector<int>(t));
}

signed main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  solve();

  return 0;
}
