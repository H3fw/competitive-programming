#include <bits/stdc++.h>

using namespace std;

#define arr std::array
#define ll long long
#define endl '\n'
#define int ll

void solve() {
  int n, r;
  cin >> n >> r;

  set<pair<int, int>> circles;
  for (int i = 0; i < n; i++) {
    int px, py;
    cin >> px >> py;
    int x1 = px / r;
    int x2 = px / r + 1
  }
}

signed main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  int t;
  cin >> t;
  while (t--) {
    solve();
  }

  return 0;
}
